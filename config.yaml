weights: weights/best_exp50_4classes.pt
device: 0 # cuda device, i.e. 0 or 0,1,2,3 or cpu
input_size: [1280, 1280] # inference size h,w
nms_conf_thres: 0.0  # confidence threshold
iou_thres: 0.3  # NMS IOU threshold
max_det: 1000  # maximum detections per image
classes: null  # filter by class: --class 0, or --class 0 2 3
agnostic_nms: True # class-agnostic NMS
augment: False  # augmented inference
half: True  # use FP16 half-precision inference
classify: False  # False
suffixes: ['.pt', '.onnx']
stride: 32
auto_letterbox: false


max_age: 200
min_hits: 3
iou_threshold: 0.3
buffer: 7